apiVersion: v1
kind: ConfigMap
metadata:
  name: dbt-project-repos
data:
  # property-like keys; each key maps to a simple value
  proyect_a: "https://randomprojecturl.github.com"
  ui_properties_file_name: "user-interface.properties"